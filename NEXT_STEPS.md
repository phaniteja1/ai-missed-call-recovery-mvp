# Next Steps

This document reflects the current deployed state as of March 6, 2026.

## Current Production State

- Vercel production is deployed.
- Twilio inbound calls are routed through `/api/webhook`.
- Vapi dynamic assistant config is generated by `/api/vapi-webhook`.
- Business context is loaded dynamically from Supabase by phone number.
- Prompt generation is modularized under `lib/prompts`.
- Assistant config generation is modularized under `lib/vapi`.
- Appointment handling now depends on:
  - `appointment_handling_enabled`
  - `calcom_enabled`
  - valid Cal.com integration credentials

## Immediate Required Action

Run the pending Supabase migration:

- `supabase/migrations/006_appointment_handling_flag.sql`

This adds:

- `businesses.appointment_handling_enabled boolean not null default false`

It also backfills existing businesses to `true`, which matches the current HVAC and Dental setup.

## Production Verification Checklist

### 1. Supabase
- Run migration `001` through `006` if production is not fully current.
- Confirm the `businesses` table includes `appointment_handling_enabled`.
- Confirm both existing businesses have:
  - `appointment_handling_enabled = true`
  - correct `timezone`
  - correct `services`
  - correct `ai_instructions`
  - correct `custom_greeting` and `custom_closing` if used

Suggested verification query:

```sql
select
  id,
  name,
  calcom_enabled,
  appointment_handling_enabled,
  timezone,
  ai_voice_preset
from public.businesses
order by created_at desc;
```

### 2. Phone Mapping
- Confirm each Twilio number exists in `business_phone_numbers`.
- Confirm the stored phone numbers exactly match the E.164 values Vapi receives.

Suggested verification query:

```sql
select
  b.name,
  p.phone_number,
  p.active,
  p.is_primary
from public.business_phone_numbers p
join public.businesses b on b.id = p.business_id
order by b.name, p.phone_number;
```

### 3. Vapi
- Assistant Server URL should be:
  - `https://ai-missed-call-recovery-mvp.vercel.app/api/vapi-webhook`
- Use one assistant as the shared dynamic assistant.
- Keep prompt/config logic in code, not in a static dashboard prompt, unless you intentionally want an override.

### 4. Twilio
- Both Twilio numbers should point voice webhook to:
  - `https://ai-missed-call-recovery-mvp.vercel.app/api/webhook`
- Method should be `POST`.

### 5. Live Call Testing
- Call the Dental number and confirm:
  - dental tone and triage language
  - appointment handling behavior matches business flags
  - call record and transcript are stored
- Call the HVAC number and confirm:
  - HVAC-specific language
  - booking or callback behavior matches business flags
  - call record and transcript are stored

## Recommended Next Build Tasks

### 1. Add dashboard controls for AI config
Expose these business fields in the dashboard:
- `appointment_handling_enabled`
- `calcom_enabled`
- `ai_instructions`
- `services`
- `specialties`
- `faq_data`
- `custom_greeting`
- `custom_closing`
- `ai_voice_preset`

### 2. Persist callback requests
`scheduleCallback` currently returns a message but does not store a structured callback request. Add persistence so missed-sales and follow-up workflows are trackable.

### 3. Add explicit business type
Prompt specialization currently infers `dental` or `hvac` from business text. Add a real DB column such as `business_type` if you want predictable prompt selection.

### 4. Add prompt QA workflow
Use `/api/debug/prompt-preview` to inspect generated prompts per business, then add a repeatable QA checklist for:
- dental intake
- dental urgent calls
- HVAC emergency-sounding calls
- booking-enabled flows
- callback-only flows

## Suggested Order

1. Run Supabase migration `006`.
2. Verify both business records and phone mappings.
3. Test both live phone numbers.
4. Add dashboard controls for AI config.
5. Persist callback requests.
